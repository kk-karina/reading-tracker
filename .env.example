# Copy to .env.local and fill in from Supabase → Project Settings → API.
# Leave both empty (or delete the file) to run in local mode (browser storage only).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
